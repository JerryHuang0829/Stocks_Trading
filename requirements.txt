# ==================================================================
# DEPLOY / DASHBOARD-only stack — Streamlit Community Cloud 用
# ------------------------------------------------------------------
# Dashboard 只讀 reports/*.json 不跑回測 / 不抓 FinMind / 不算技術指標。
# 故只需 4 個 runtime 套件（pandas / scipy / streamlit / plotly），
# 完全避開 pandas-ta + numba + llvmlite 等 JIT 套件 — 它們 build 時
# 需要 Python 3.10-3.13，但 Streamlit Cloud 新 uv installer 預設可能
# 用更新版 Python 導致 build fail。
#
# 本機 dev / pytest / 資料抓取 / 回測請改用 requirements-dev.txt
# （全套 deps，需要 conda env Python 3.12）。
# ==================================================================
pandas>=2.0.0
scipy>=1.11.0
streamlit>=1.30.0
plotly>=5.18.0
